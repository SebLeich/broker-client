import { ServiceproviderPipe } from './serviceprovider.pipe';

describe('ServiceproviderPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceproviderPipe();
    expect(pipe).toBeTruthy();
  });
});
