import { ServicemodelPipe } from './servicemodel.pipe';

describe('ServicemodelPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicemodelPipe();
    expect(pipe).toBeTruthy();
  });
});
