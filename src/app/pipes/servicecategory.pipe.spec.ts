import { ServicecategoryPipe } from './servicecategory.pipe';

describe('ServicecategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicecategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
